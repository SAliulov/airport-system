-- =============================================
-- Добавление версии для оптимистичной блокировки
-- (NFR: Надёжность — защита от конкурентного
--  редактирования рейса двумя диспетчерами)
-- =============================================
ALTER TABLE flight
    ADD COLUMN version BIGINT NOT NULL DEFAULT 0;