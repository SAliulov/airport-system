/**
 * Сквозная функциональность (AOP): аудит и логирование вызовов сервисного слоя и т.п.
 * Не содержит предметных правил рейсов/гейтов — только обёртка вокруг уже принятых решений в {@code service}.
 */
package ru.airport.aspect;
