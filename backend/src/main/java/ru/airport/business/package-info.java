/**
 * Слой бизнес-логики (FirstLab §3.3): правила предметной области без JPA-репозиториев и без DTO.
 * Вызывается из прикладных сервисов ({@code ru.airport.service}) после загрузки сущностей.
 */
package ru.airport.business;
