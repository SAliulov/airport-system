/**
 * Spring Data JPA: интерфейсы репозиториев и при необходимости спецификации/кастомные запросы.
 * Только доступ к данным, без прикладной и доменной логики (AGENTS.md раздел 2.1).
 */
package ru.airport.repository;
