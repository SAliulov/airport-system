/**
 * WebSocket/STOMP: конфигурация брокера сообщений и сервис рассылки клиентам при изменениях в системе.
 * Тела сообщений — в подпакете {@link ru.airport.websocket.payload}; бизнес-операции остаются в {@code service}.
 */
package ru.airport.websocket;
